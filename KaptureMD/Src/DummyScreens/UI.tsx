import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const UI = () => {
  return (
    <View>
      <Text>UI</Text>
    </View>
  )
}

export default UI

const styles = StyleSheet.create({})